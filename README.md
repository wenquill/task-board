# TaskBoard

**How to run the app locally** 
run `npm install` to install dependencies
run `npm run dev`
open the URL (e.g. http://localhost:5173).

**The app is hosted on Vercel**
https://todo-list-eight-weld-33.vercel.app/